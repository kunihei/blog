.main-sign
  .main-sign_logo
    ユーザー登録
  .main-sign_up
    = form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f|
      = render "devise/shared/error_messages", resource: resource
      .field
        .main-sign_up_nickname
          = f.label "ニックネーム"
          %br/
          = f.text_field :nickname, autofocus: true, class: 'main-sign_up_box', placeholder:'名前を入力してくだい'
        .field
          .main-sign_up_email
            = f.label :email
            %br/
            = f.email_field :email, autofocus: true, autocomplete: "email", class: 'main-sign_up_box', placeholder:'アドレスを入力してください'
        .field
          .main-sign_up_password
            = f.label :password
            - if @minimum_password_length
              %em
                (#{@minimum_password_length} 文字以上)
            %br/
            = f.password_field :password, autocomplete: "new-password", id: "js-password", class: 'main-sign_up_pass', placeholder:'パスワードを入力してくだい'
        .field
          .main-sign_up_password2
            = f.label "Password(確認)"
            %br/
            = f.password_field :password_confirmation, autocomplete: "new-password", class: 'main-sign_up_pass', placeholder:'もう一度入力してください'
        .actions
          = f.submit "登録",class: 'btn'
